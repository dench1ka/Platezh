<Window x:Class="Platezh.Views.EditClientWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Редактирование клиента" Height="450" Width="450" 
        WindowStartupLocation="CenterOwner"
        Icon="/Assets/Icons/logo.ico"
        FontSize="14"
        Background="#FFF5F9FF">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#FF1976D2"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>

        <Style TargetType="Label">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Height" Value="32"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Margin" Value="5"/>
        </Style>

        <Style TargetType="DatePicker">
            <Setter Property="Height" Value="32"/>
            <Setter Property="Margin" Value="5"/>
        </Style>
    </Window.Resources>

    <Border Margin="10" Background="White" CornerRadius="5" BorderBrush="#FFE0E0E0" BorderThickness="1" Padding="15">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="130"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Label Content="Фамилия:" Grid.Row="0" Grid.Column="0"/>
            <TextBox x:Name="SurnameBox" Grid.Row="0" Grid.Column="1"/>

            <Label Content="Имя:" Grid.Row="1" Grid.Column="0"/>
            <TextBox x:Name="NameBox" Grid.Row="1" Grid.Column="1"/>

            <Label Content="Отчество:" Grid.Row="2" Grid.Column="0"/>
            <TextBox x:Name="FatherNameBox" Grid.Row="2" Grid.Column="1"/>

            <Label Content="№ Паспорта:" Grid.Row="3" Grid.Column="0"/>
            <TextBox x:Name="PassportDataBox" Grid.Row="3" Grid.Column="1"/>

            <Label Content="Кем выдан:" Grid.Row="4" Grid.Column="0"/>
            <TextBox x:Name="IssuedByBox" Grid.Row="4" Grid.Column="1"/>

            <Label Content="Дата выдачи:" Grid.Row="5" Grid.Column="0"/>
            <DatePicker x:Name="DateIssued" Grid.Row="5" Grid.Column="1"/>

            <Label Content="Адрес:" Grid.Row="6" Grid.Column="0"/>
            <TextBox x:Name="AddressBox" Grid.Row="6" Grid.Column="1"/>

            <StackPanel Grid.Row="8" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
                <Button Content="Сохранить" Width="110" Height="35" Background="#FF43A047" Click="SaveBtn_Click"/>
                <Button Content="Отмена" Width="110" Height="35" Background="#FF757575" Click="CancelBtn_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>