<Window x:Class="Platezh.Views.AdminWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Администратор" Height="830" Width="1550" WindowState="Maximized"
        Icon="/Assets/Icons/logo.ico" FontSize="14">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#FF1976D2"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="5"/>
        </Style>
    </Window.Resources>

    <Grid Background="#FFF5F9FF" Margin="8">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="300"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Margin="0,10,10,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                <StackPanel Width="300" Margin="0,0,20,0">
                    <Label Content="Поиск по логину"/>
                    <TextBox x:Name="SearchBox" TextChanged="SearchBox_TextChanged"/>
                </StackPanel>
            </StackPanel>

            <Label Grid.Row="1" Content="Пользователи" FontSize="20" FontWeight="Bold"/>

            <Border Grid.Row="2" CornerRadius="5" BorderBrush="#FFE0E0E0" BorderThickness="1" Background="White">
                <DataGrid x:Name="UsersGrid"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          SelectionMode="Single"
                          IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding id}" Width="60"/>
                        <DataGridTextColumn Header="Логин" Binding="{Binding login}" Width="*"/>
                        <DataGridTextColumn Header="Роль" Binding="{Binding roleName}" Width="150"/>
                        <DataGridTextColumn Header="Активен" Binding="{Binding isActive}" Width="100"/>
                        <DataGridTextColumn Header="Дата создания" Binding="{Binding createdAt}" Width="200"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>

        <Border Grid.Column="1" Background="White" CornerRadius="5" BorderBrush="#FFE0E0E0" BorderThickness="1" Margin="10,10,0,10" Padding="12">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="Управление пользователем" FontWeight="Bold" Foreground="#FF1976D2" FontSize="16" Margin="0,0,0,10"/>

                    <Button Content="Добавить пользователя" Click="AddUser_Click" Background="#FF43A047" Margin="0,15,0,0" Height="40" FontWeight="Bold"/>
                    <Button Content="Редактировать пользователя" Click="EditUser_Click" Background="#FF1976D2" Margin="0,15,0,0" Height="40" FontWeight="Bold"/>
                    <Button Content="Удалить пользователя" Click="DeleteUser_Click" Background="#FFD32F2F" Margin="0,15,0,0" Height="40" FontWeight="Bold"/>

                    <Separator Margin="0,15"/>
                    <Button Content="Назад" Click="Back_Click" Background="#FF757575"/>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</Window>