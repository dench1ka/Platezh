<Window x:Class="Platezh.Views.EditUserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Редактирование пользователя"
        Height="300" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="White">

    <Border CornerRadius="8" BorderBrush="#FFCCCCCC" BorderThickness="1" Padding="15" Background="White">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Заголовок -->
            <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Text="Редактирование пользователя" 
                       FontSize="16" FontWeight="Bold" Margin="0,0,0,15"/>

            <!-- Логин -->
            <TextBlock Grid.Row="1" Grid.Column="0" Text="Логин:" VerticalAlignment="Center"/>
            <TextBox x:Name="LoginTextBox" Grid.Row="1" Grid.Column="1" Margin="5" Height="25" Padding="4"/>

            <!-- Пароль -->
            <TextBlock Grid.Row="2" Grid.Column="0" Text="Пароль:" VerticalAlignment="Center"/>
            <PasswordBox x:Name="PasswordBox" Grid.Row="2" Grid.Column="1" Margin="5" Height="25" Padding="4"/>

            <!-- Активность -->
            <TextBlock Grid.Row="3" Grid.Column="0" Text="Статус:" VerticalAlignment="Center"/>
            <CheckBox x:Name="IsActiveCheckBox" Grid.Row="3" Grid.Column="1" Content="Активен" Margin="5"/>

            <!-- Кнопки -->
            <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button x:Name="SaveButton" Content="Сохранить" Width="100" Height="30" Margin="5" Background="#4CAF50" Foreground="White" Click="SaveButton_Click"/>
                <Button Content="Отмена" Width="100" Height="30" Margin="5" Background="#F44336" Foreground="White" Click="CancelButton_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
